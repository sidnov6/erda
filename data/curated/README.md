# data/curated

Hand-maintained inputs (fiscal regimes, cost benchmarks, licensing rounds,
financing exclusions, outcome maps, EIA series pins).

**Rule (spec §7):** hand-maintained is fine — *uncited* is not. Every row/field
carries a `source_url`. The contract validator rejects curated rows without one.
