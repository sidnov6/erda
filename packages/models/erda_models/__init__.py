"""ERDA models: GBM ranker, patch CNN, calibration, evaluation, model cards (spec §9).

Spatial CV with 50 km exclusion buffer is non-negotiable; the falsification gate
(§9.8) blocks shipping a map without demonstrated skill.
"""

__version__ = "0.1.0"
