"""ERDA agents: LangGraph feasibility committee (spec §10).

Typed tools over local DuckDB/Zarr only — no live internet at memo time. Every tool
return includes source_ids. No model call performs arithmetic.
"""

__version__ = "0.1.0"
