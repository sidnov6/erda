# ops

Operational scripts (demo_freeze.py, refresh helpers) live here.
GitHub Actions workflows live in `.github/workflows/` (Actions only reads that
path); `ci.yml` and `nightly_refresh.yml` are found there, not in ops/.
