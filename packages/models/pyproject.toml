[project]
name = "erda-models"
version = "0.1.0"
description = "Prospectivity models: GBM, patch CNN, calibration, evaluation, model cards."
requires-python = ">=3.11,<3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["erda_models"]
