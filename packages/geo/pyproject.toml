[project]
name = "erda-geo"
version = "0.1.0"
description = "Raster stack builder, co-registration, derived channels. DETERMINISTIC CORE."
requires-python = ">=3.11,<3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["erda_geo"]
