[project]
name = "erda-agents"
version = "0.1.0"
description = "LangGraph feasibility committee: graph, typed tools, prompts, memo schema."
requires-python = ">=3.11,<3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["erda_agents"]
