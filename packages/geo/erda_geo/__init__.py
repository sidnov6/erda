"""ERDA geo: raster stack builder, co-registration, derived channels (spec §6).

DETERMINISTIC CORE (spec §0 rule 2): pure Python — no network calls, no unseeded
randomness, no LLM calls. Same inputs, identical outputs.
"""

__version__ = "0.1.0"
